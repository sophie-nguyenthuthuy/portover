name: CI
on:
  push:
    branches: [main]
  pull_request:

jobs:
  test:
    runs-on: ubuntu-latest
    strategy:
      matrix:
        python-version: ["3.11", "3.12", "3.13"]
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-python@v5
        with:
          python-version: ${{ matrix.python-version }}
      # pyyaml is TEST-ONLY: the tests validate portover's hand-rolled YAML
      # emitter against a real parser. portover itself has zero dependencies.
      - run: pip install pytest pyyaml
      - run: python -m pytest tests/ -q
      - name: docs are up to date
        run: |
          python -m portover.cli docs --out /tmp/docs-check
          diff -r docs /tmp/docs-check
